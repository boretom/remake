image: freebsd/12.0
packages:
    - devel/gmake
    - ftp/wget
    - archivers/lzip
sources:
- https://github.com/rocky/remake.git
tasks:
    - configure |
        cd remake
		git checkout remake-4-3
        autoreconfig -i
        ./configure --prefix=${HOME}
    - build: |
        gmake po-update
		(cd doc && make stamp-1 stamp-vti)
        gmake

